name: Rust

on:
  push:
    branches: [ "stable", "ai-beta" ]
  pull_request:
    branches: [ "stabel" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --release -v
